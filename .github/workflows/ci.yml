name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Validate project structure
      run: |
        test -f configs/decision_config.json
        test -f models/calibrated_xgb.pkl

    - name: Test API loads
      run: |
        python -c "from src.api.app import app; print('API loaded successfully')"

    - name: Check pipeline file exists
      run: |
        test -f src/pipeline/run_pipeline.py

    - name: Build Docker image
      run: |
        docker build -t churn-api:test .

    - name: Install dev dependencies
      run: |
        pip install -r requirements-dev.txt

    - name: Run tests
      run: |
        pytest tests/